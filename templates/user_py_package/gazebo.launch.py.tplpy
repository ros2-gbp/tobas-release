# Do not delete or rename this file because it is executed in {{ config_pkg_name }}/gazebo.launch.xml.

from launch import LaunchDescription


def generate_launch_description():
    ld = LaunchDescription()

    # Please add nodes that run only during the Gazebo simulation.

    return ld
