# Do not delete or rename this file because it is executed in {{ config_pkg_name }}/real_interface.launch.py.

from launch import LaunchDescription


def generate_launch_description():
    ld = LaunchDescription()

    # Please add nodes that runs only on real hardware.

    return ld
