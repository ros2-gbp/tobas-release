#!/bin/bash

env \
  DISPLAY=$DISPLAY \
  XAUTHORITY=$XAUTHORITY \
  USER=$(id -nu "$PKEXEC_UID") \
gnome-terminal -- bash -lc '
  /usr/bin/tobas_install_prereqs
  status=$?
  echo
  if (( status != 0 )); then
    echo ">>> Exit code: $status"
  fi
  read -rp "Press Enter to close..."
'
